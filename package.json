{
    "name": "fluid",
    "version": "0.0.1",
    "dependencies": {
        "dat.gui": "^0.7.7",
        "regl": "^2.1.0"
    },
    "scripts": {
        "start": "snowpack dev",
        "build": "snowpack build && touch build/.nojekyll"
    },
    "devDependencies": {
        "snowpack": "^3.3.5",
        "snowpack-plugin-raw-file-loader": "^1.0.9"
    }
}
